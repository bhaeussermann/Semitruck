# Semitruck

A simple top-down game written in Go using the [Ebitengine](ebitengine.org) game engine.

## Usage

To run the program, execute the following command from the repository root:

```
cd src && go run main.go
```
